[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "flash-dtof-simulator"
version = "0.6.0"
description = "NYU-geometry and STB-timing first-photon Flash dToF array simulator"
readme = "README.md"
requires-python = ">=3.8"
authors = [{name = "Flash dToF simulator contributors"}]
dependencies = [
  "numpy>=1.24,<2",
  "Pillow>=10,<11",
]

[tool.setuptools.packages.find]
include = ["flash_dtof*"]
